# No environment variables are required to preview the site locally.

# Set this when the deployment uses Netlify Forms.
VITE_FORM_PROVIDER=netlify

# For Shopify, Vercel, or another host, point sticker requests at a secure
# multipart form endpoint that returns a non-2xx response when delivery fails.
VITE_STICKER_QUOTE_ENDPOINT=
